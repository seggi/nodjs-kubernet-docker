apiVersion: v1
kind: Service
metadata:
  name: {{ include "nk-k02.fullname" . }}
  labels:
    {{- include "nk-k02.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nk-k02.selectorLabels" . | nindent 4 }}
